# Contributing

1. Read AGENTS.md first.
2. Create a branch: `feat|fix/<agent>/<short-slug>`.
3. Implement with tests.
4. Run CI: `ruff`, `mypy`, `pytest`.
5. Open a PR with description of what changed and what was verified.
